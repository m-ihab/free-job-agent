"""Domain-grouped CLI command handlers for free-job-agent.

Handler callables live in the per-domain modules in this package and are wired
into the argument parser by :mod:`job_agent.cli.main`. Shared helpers live in
:mod:`job_agent.cli.commands._common`.
"""
